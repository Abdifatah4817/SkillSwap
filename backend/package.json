{
  "name": "skillswap",
  "version": "1.0.0",
  "description": "SkillSwap backend using json-server",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
